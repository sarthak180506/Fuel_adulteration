# ── Core scientific stack ───────────────────────────────────────────────────
numpy>=2.0
pandas>=2.2
scipy>=1.13

# ── Machine Learning ────────────────────────────────────────────────────────
scikit-learn>=1.5
xgboost>=2.1

# ── Visualisation ───────────────────────────────────────────────────────────
matplotlib>=3.9
seaborn>=0.13

# ── Experiment tracking ─────────────────────────────────────────────────────
mlflow>=2.15

# ── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.3
pytest-cov>=5.0

# ── Code quality ────────────────────────────────────────────────────────────
ruff>=0.6

# ── Optional (local dev only, not needed for CI tests) ──────────────────────
# tensorflow[cpu]>=2.17          # TFLite conversion — install manually
# ai-edge-torch>=0.2             # TFLite conversion — install manually
# jupyter>=1.1                   # Notebooks — install manually
# plotly>=5.22                   # Interactive plots — install manually
